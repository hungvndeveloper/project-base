Tôi sẽ giải thích chi tiết về BEM và best practices khi triển khai HTML/CSS dựa trên code mẫu của bạn.
    1. BEM (Block Element Modifier) là gì?
        BEM là một phương pháp đặt tên class CSS với cú pháp:
            - Block: Thành phần độc lập (ví dụ: header, search)
            - Element: Phần tử con của block (headerlogo, search_input)
            - Modifier: Biến thể của block/element (headerbutton--user)
        Cú pháp:
            block__element--modifier
    2. Ví dụ BEM trong code của bạn:
        <!-- Block: header -->
        <header class="header">
            <!-- Element: header__content -->
            <div class="header__content">
                <!-- Element: header__logo -->
                <img class="header__logo">
                
                <!-- Block mới: search -->
                <div class="search header__search">
                    <!-- Element của search -->
                    <i class="search__icon"></i>
                    <input class="search__input">
                </div>

                <!-- Element với modifier -->
                <button class="header__button header__button--user">
        </header>
    3. Best Practices khi viết HTML/CSS:
        - Semantic HTML:
            Sử dụng các thẻ có ý nghĩa: <header>, <nav>, <main>, <footer>
            Tránh lạm dụng <div> khi có thể dùng thẻ semantic
        - Accessibility:
            <!-- Thêm aria-label cho các button -->
            <button class="header__button" aria-label="User account">
                <i class="fa-regular fa-user"></i>
            </button>

            <!-- Thêm alt cho hình ảnh -->
            <img class="header__logo" alt="Coolmate logo" src="...">
        - Cấu trúc CSS:
            /* Tổ chức CSS theo thứ tự */
            .header {
                /* 1. Positioning */
                position: relative;
                
                /* 2. Box Model */
                padding: 20px;
                margin: 10px;
                
                /* 3. Visual */
                background: white;
                
                /* 4. Typography */
                font-size: 16px;
                
                /* 5. Misc */
                transition: all 0.3s;
            }
        - Mobile First:
            /* Mobile first */
            .header__menu {
                display: none; /* Mặc định ẩn trên mobile */
            }

            /* Desktop styles */
            @media (min-width: 768px) {
                .header__menu {
                    display: flex;
                }
            }
    4. Cải thiện code hiện tại:
        - Tách components:
            <!-- Tách search thành component riêng -->
            <div class="search">
                <i class="search__icon"></i>
                <input class="search__input" type="text">
                <button class="search__button" aria-label="Search">
            </div>
        - Thêm các thuộc tính accessibility:
            <nav class="header__nav" role="navigation">
            <button class="header__button" aria-label="Toggle menu">
            <input class="search__input" aria-label="Search products" type="text">
        - Tổ chức file CSS:
            styles/
            ├── base/
            │   ├── reset.css
            │   └── typography.css
            ├── components/
            │   ├── header.css
            │   └── search.css
            └── main.css
    5. Lưu ý quan trọng:
        - Luôn giữ tính nhất quán trong việc đặt tên
        - Tránh selector quá sâu (không quá 3 cấp)
        - Tách code thành các component nhỏ, tái sử dụng
        - Ưu tiên mobile-first và responsive design
        - Luôn nghĩ đến accessibility khi code
        - Comment code khi cần thiết để giải thích logic phức tạp
    
=============================================================================================
vậy nếu như có nhiều cấp lồng nhau thì làm sao để biết đâu là block, element, modifier

=> Tôi sẽ giải thích cách xác định Block, Element và Modifier trong các trường hợp lồng nhau phức tạp.
    1. Nguyên tắc cơ bản:
        - Block là thành phần độc lập, có thể tái sử dụng
        - Element là thành phần phụ thuộc vào Block
        - Modifier là biến thể của Block hoặc Element
    2. Ví dụ với menu phức tạp:
        <nav class="menu">                              <!-- Block: menu -->
            <ul class="menu__list">                     <!-- Element của menu -->
                <li class="menu__item">                 <!-- Element của menu -->
                    <a href="#" class="menu__link">     <!-- Element của menu -->
                        <span class="menu__text">       <!-- Element của menu -->
                            Products
                        </span>
                        <div class="submenu">           <!-- Block mới: submenu (KHÔNG phải menu__submenu) -->
                            <ul class="submenu__list">  <!-- Element của submenu -->
                                <li class="submenu__item submenu__item--active"> <!-- Element với modifier -->
                                    <a href="#" class="submenu__link">
                                        Sport
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </a>
                </li>
            </ul>
        </nav>
3. Các trường hợp phức tạp:
    - Card với nhiều thành phần:
        <form class="form">                       <!-- Block: form -->
            <div class="form__group">             <!-- Element của form -->
                <label class="form__label">       <!-- Element của form -->
                    Email
                </label>
                
                <div class="input">               <!-- Block mới: input -->
                    <input class="input__field input__field--error"> <!-- Element với modifier -->
                    <span class="input__icon">    <!-- Element của input -->
                        <i class="fa fa-envelope"></i>
                    </span>
                    <div class="tooltip">         <!-- Block mới: tooltip -->
                        <span class="tooltip__text">  <!-- Element của tooltip -->
                            Invalid email
                        </span>
                    </div>
                </div>
            </div>
        </form>
    - Form phức tạp:
        <form class="form">                       <!-- Block: form -->
            <div class="form__group">             <!-- Element của form -->
                <label class="form__label">       <!-- Element của form -->
                    Email
                </label>
        
                <div class="input">               <!-- Block mới: input -->
                    <input class="input__field input__field--error"> <!-- Element với modifier -->
                    <span class="input__icon">    <!-- Element của input -->
                        <i class="fa fa-envelope"></i>
                    </span>
                    <div class="tooltip">         <!-- Block mới: tooltip -->
                        <span class="tooltip__text">  <!-- Element của tooltip -->
                            Invalid email
                        </span>
                    </div>
                </div>
            </div>
        </form>
4. Quy tắc xác định Block mới:
    - Khi nào tạo Block mới:
        - Thành phần có thể tái sử dụng ở nhiều nơi
        - Thành phần hoạt động độc lập
        - Thành phần có logic/style riêng biệt
    - Khi nào dùng Element:
        - Phần tử phụ thuộc vào Block cha
        - Không thể tồn tại độc lập
        - Chỉ sử dụng trong phạm vi Block
5. Tips để tránh nhầm lẫn:
    - Không lồng BEM:
        /* KHÔNG NÊN */
        .block__element1__element2 { }

        /* NÊN */
        .block__element1 { }
        .block__element2 { }
    - Tạo Block mới khi cần:
        <!-- KHÔNG NÊN -->
        <div class="card__header__title__text">

    <!-- NÊN -->
        <div class="card__header">
            <h2 class="card__title">
                <span class="text">  <!-- Block mới nếu text có style riêng -->
    - Modifier rõ ràng:
        <!-- KHÔNG NÊN -->
        <div class="card card__header--blue">

        <!-- NÊN -->
        <div class="card card--blue">
            <div class="card__header">
    - Kiểm tra tính độc lập:
        - Nếu có thể di chuyển component đến vị trí khác mà vẫn hoạt động → Block mới
        - Nếu không thể tách rời khỏi parent → Element
=============================================================================================
<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Meta tags cho SEO và responsive -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- Liên kết các file CSS -->
    <link rel="stylesheet" href="reset.css">  <!-- Reset CSS để chuẩn hóa styles across browsers -->
    <link rel="stylesheet" href="style.css">  <!-- File CSS chính của project -->
    
    <!-- Font Awesome cho icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400|Rufina:400,700&display=swap" rel="stylesheet">
    
    <title>Coolmate copy</title>
</head>

<body>
    <!-- Wrapper chính của website -->
    <div class="site-wrapper">
        <!-- Header section -->
        <header class="header">
            <!-- Container để giới hạn width của content -->
            <div class="container">
                <!-- Header content wrapper -->
                <div class="header__content">
                    <!-- Logo -->
                    <img class="header__logo" src="https://www.coolmate.me/images/logo-coolmate-new.svg?v=1" alt="Coolmate logo">
                    
                    <!-- Navigation chính -->
                    <nav class="header__nav">
                        <!-- Menu chính -->
                        <ul class="header__menu">
                            <!-- Các menu items -->
                            <li class="header__menu-item">
                                <a href="#sale" class="header__menu-link">Giảm đến 50%</a>
                            </li>
                            <li class="header__menu-item">
                                <a href="#products" class="header__menu-link">Sản phẩm</a>
                            </li>
                            <li class="header__menu-item">
                                <a href="#underwear" class="header__menu-link">Đồ lót</a>
                            </li>
                            <li class="header__menu-item">
                                <a href="#sport" class="header__menu-link">Đồ thể thao</a>
                            </li>
                            <li class="header__menu-item">
                                <a href="#daily" class="header__menu-link">Mặc hàng ngày</a>
                            </li>
                            <li class="header__menu-item">
                                <a href="#custom" class="header__menu-link">Sản xuất riêng</a>
                            </li>
                            <li class="header__menu-item">
                                <a href="#care" class="header__menu-link">CARE & SHARE</a>
                            </li>
                        </ul>
                    </nav>

                    <!-- Header actions (search, user, cart) -->
                    <div class="header__actions">
                        <!-- Search component -->
                        <div class="search header__search">
                            <!-- Search icon -->
                            <i class="search__icon fa-solid fa-magnifying-glass"></i>
                            <!-- Search input -->
                            <input type="text" class="search__input" placeholder="Tìm kiếm sản phẩm">
                        </div>
                        
                        <!-- User account button -->
                        <button class="header__button header__button--user" aria-label="User account">
                            <i class="fa-regular fa-user"></i>
                        </button>
                        
                        <!-- Shopping cart button -->
                        <button class="header__button header__button--cart" aria-label="Shopping cart">
                            <i class="fa-solid fa-cart-shopping"></i>
                        </button>
                    </div>
                </div>
            </div>
        </header>
    </div>
</body>

</html>
===============================================================================
giải thích chi tiết các cấu trúc của file index.html này

Tôi sẽ giải thích chi tiết cấu trúc của file index.html:
    1. Cấu trúc cơ bản HTML5:
        <!DOCTYPE html>
        <html lang="en">
            <head>
                <!-- Chứa các meta tags và resources -->
            </head>
            <body>
                <!-- Chứa nội dung trang web -->
            </body>
        </html>
    2. Phần Head (Meta & Resources):
        <head>
            <!-- SEO & Responsive Meta Tags -->
            <meta charset="UTF-8">  <!-- Định dạng ký tự UTF-8 -->
            <meta name="viewport" content="width=device-width, initial-scale=1.0">  <!-- Cấu hình responsive -->
            
            <!-- CSS Files -->
            <link rel="stylesheet" href="reset.css">  <!-- Reset default browser styles -->
            <link rel="stylesheet" href="style.css">  <!-- Custom styles -->
            
            <!-- Third-party Resources -->
            <!-- Font Awesome - Thư viện icon -->
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
            
            <!-- Google Fonts - Typography -->
            <link href="https://fonts.googleapis.com/css?family=Roboto:300,400|Rufina:400,700&display=swap" rel="stylesheet">
        </head>
   3. Cấu trúc Body theo BEM:
        <body>
            <!-- Site Wrapper - Block độc lập -->
            <div class="site-wrapper">
                <!-- Header Component - Block chính -->
                <header class="header">
                    <!-- Container - Block layout -->
                    <div class="container">
                        <!-- Header Content - Element của header -->
                        <div class="header__content">
                            <!-- Các thành phần con -->
                        </div>
                    </div>
                </header>
            </div>
        </body>

    4. Header Component:
        <!-- Logo Element -->
        <img class="header__logo" src="..." alt="Coolmate logo">

        <!-- Navigation Component -->
        <nav class="header__nav">
            <ul class="header__menu">
                <li class="header__menu-item">
                    <a href="#" class="header__menu-link">Menu Item</a>
                </li>
                <!-- Các menu items khác -->
            </ul>
        </nav>

        <!-- Actions Component -->
        <div class="header__actions">
            <!-- Search Block -->
            <div class="search header__search">
                <i class="search__icon fa-solid fa-magnifying-glass"></i>
                <input type="text" class="search__input" placeholder="Tìm kiếm sản phẩm">
            </div>
            
            <!-- User & Cart Buttons -->
            <button class="header__button header__button--user">...</button>
            <button class="header__button header__button--cart">...</button>
        </div>

    5. Phân tích BEM trong code:
        - Blocks:
            - site-wrapper: Block wrapper chính
            - header: Block header
            - container: Block layout
            - search: Block độc lập cho chức năng search
        - Elements:
            - header__content: Element của header
            - header__logo: Element logo của header
            - header__nav: Element navigation
            - header__menu: Element menu
            - header__menu-item: Element menu item
            - header__menu-link: Element link trong menu
            - header__actions: Element chứa các actions
            - header__button: Element button trong header
            - search__icon: Element icon của search
            - search__input: Element input của search
        - Modifiers:
            - header__button--user: Modifier cho user button
            - header__button--cart: Modifier cho cart button
    6. Accessibility Features:
        <!-- ARIA labels cho buttons -->
        <button class="header__button" aria-label="User account">
            <i class="fa-regular fa-user"></i>
        </button>

        <!-- Alt text cho images -->
        <img class="header__logo" alt="Coolmate logo" src="...">

        <!-- Semantic HTML -->
        <header>, <nav>, <button>
    7. Responsive Considerations:
        - Viewport meta tag cho responsive design
        - Container class để kiểm soát layout
        - Flexbox layout cho responsive navigation
        - BEM structure cho dễ dàng style responsive
    8. Cấu trúc này tuân thủ:
        - Semantic HTML5
        - BEM Methodology
        - Accessibility standards
        - Responsive design principles
        - Clean code practices
################################################################################
:root {
    /* Colors */
    --primary-color: #ff817e;
    --text-color: #231f20;
    --gray-dark: #263a41;
    --gray-light: #888;
    --white: #FFFFFF;
    --black: #000000;
    --bg-block: #fff9f8;
    --header-bg: #212121;

    /* Typography */
    --primary-font: "Roboto", sans-serif;
    --secondary-font: "Rufina", serif;
    
    /* Layout */
    --wrapper-width: 192rem;
    --container-width: 177.5rem;
    
    /* Transitions */
    --transition-base: all 0.3s ease-in-out;

    /* Breakpoints */
    --breakpoint-mobile: 480px;
    --breakpoint-tablet: 768px;
    --breakpoint-desktop: 1024px;
}

*,
*:before,
*:after {
    box-sizing: border-box;
}

html {
    font-size: 62.5%;
}

body {
    font-family: var(--primary-font);
    font-weight: 400;
    color: var(--text-color);
    background-color: var(--white);
    line-height: 2.1rem;
}

img {
    display: block;
    max-width: 100%;
}

a {
    display: block;
    text-decoration: none;
}

.site-wrapper {
    max-width: var(--wrapper-width);
    margin-left: auto;
    margin-right: auto;
    overflow-x: hidden;
}

.container {
    max-width: var(--container-width);
    margin-left: auto;
    margin-right: auto;
    padding: 0 15px;
}

.header {
    background-color: #212121;
    padding: 10px 0;
}

.header__content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin: 0 auto;
    max-width: 139rem;
    padding: 0 1.5rem;
}

.header__logo {
    max-width: 150px;
}

.header__nav {
    flex-grow: 1;
    margin: 0 20px;
}

.header__menu {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 1rem;
}

.header__menu-item {
    margin: 0 1rem;
}

.header__menu-item a {
    color: var(--white);
    font-size: 1.4rem;
    font-weight: 400;
}

.header__menu-item a:hover,
.header__button:hover {
    color: var(--primary-color);
    transition: var(--transition-base);
}

.header__actions {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 1rem;
}

.header__search {
    position: relative;
    margin-right: 1rem;
}

.header__search i {
    color: var(--white);
    font-size: 2rem;
    position: absolute;
    left: 10px;
    top: 50%;
    transform: translateY(-50%);
}

.header__search input {
    border: none;
    outline: none;
    color: var(--white);
    font-size: 1.4rem;
    font-weight: 400;
    background-color: transparent;
    padding: 8px 10px 8px 40px;
}

.header__button {
    background: none;
    border: none;
    color: var(--white);
    font-size: 2rem;
    margin-left: 1rem;
    cursor: pointer;
}

/* Mobile menu button */
.mobile-menu-button {
    display: none;
    background: none;
    border: none;
    color: var(--white);
    font-size: 2.4rem;
    cursor: pointer;
}

/* Responsive styles */
@media screen and (max-width: 1024px) {
    html {
        font-size: 55%;
    }
}

@media screen and (max-width: 768px) {
    .header__content {
        flex-wrap: wrap;
        gap: 1.5rem;
    }

    .header__logo {
        order: 1;
    }

    .header__nav {
        order: 3;
        width: 100%;
        margin: 1.5rem 0 0;
    }

    .header__menu {
        flex-wrap: wrap;
        justify-content: flex-start;
        gap: 1rem;
    }

    .header__menu-item {
        margin: 5px 10px 5px 0;
    }

    .header__actions {
        order: 2;
    }

    .header__search input {
        width: 150px;
    }
}

@media screen and (max-width: 480px) {
    .mobile-menu-button {
        display: block;
        order: 2;
    }

    .header__nav {
        display: none;
        transition: var(--transition-base);
    }

    .header__nav.is-active {
        display: block;
    }

    .header__actions {
        order: 3;
    }

    .header__search {
        display: none;
    }

    .header__button {
        font-size: 1.8rem;
    }
}

.search {
    position: relative;
    display: flex;
    align-items: center;
}

.search__icon {
    color: var(--white);
    font-size: 2rem;
    position: absolute;
    left: 10px;
    top: 50%;
    transform: translateY(-50%);
}

.search__input {
    border: none;
    outline: none;
    color: var(--white);
    font-size: 1.4rem;
    font-weight: 400;
    background-color: transparent;
    padding: 8px 10px 8px 40px;
    width: 200px;
}

.search__input::placeholder {
    color: var(--white);
    opacity: 0.7;
}

/* Cải thiện focus states */
.header__menu-link:focus,
.header__button:focus,
.search__input:focus {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
}

/* Thêm styles cho keyboard navigation */
.header__menu-link:focus-visible,
.header__button:focus-visible {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
}

/* Thêm reduced motion cho người dùng có vấn đề về chuyển động */
@media (prefers-reduced-motion: reduce) {
    .header__menu-item a,
    .header__button,
    .header__nav {
        transition: none;
    }
}

/* Sử dụng mobile-first approach */
@media screen and (min-width: 481px) {
    .mobile-menu-button {
        display: none;
    }
    
    .header__nav {
        display: block;
    }
}

==================================================================
1. Tổ chức CSS Variables:
    :root {
        /* Nhóm theo chức năng */
        /* Colors */
        /* Typography */
        /* Layout */
        /* Transitions */
        /* Breakpoints */
    }
2. Cấu trúc file CSS nên theo thứ tự:
    /* 1. Reset/Normalize */
    /* 2. Base styles (typography, base elements) */
    /* 3. Layout/Grid */
    /* 4. Components */
    /* 5. Utilities */
    /* 6. Media queries */
3. Quy tắc đặt tên class theo BEM:
    .block {}                     /* Block: header */
    .block__element {}           /* Element: header__menu */
    .block__element--modifier {} /* Modifier: header__menu--active */
4. Mobile-first Approach:
    /* Base styles cho mobile trước */
    .element {
        width: 100%;
    }

    /* Sau đó mới đến tablet và desktop */
    @media (min-width: 768px) {
        .element {
            width: 50%;
        }
    }
5. Tối ưu Performance:
    /* Sử dụng các thuộc tính hiệu quả */
    /* Thay vì */
    .element {
        box-shadow: 0 1px 2px rgba(0,0,0,.2);
        transform: translate3d(0,0,0);
    }

    /* Nên dùng */
    .element {
        will-change: transform;
        transform: translateZ(0);
    }
6. Accessibility:
    /* Focus states */
    :focus {
        outline: 2px solid var(--primary-color);
        outline-offset: 2px;
    }

    /* Reduced motion */
    @media (prefers-reduced-motion: reduce) {
        * {
            transition: none !important;
        }
    }
7. Tổ chức Media Queries:
    /* Định nghĩa breakpoints */
    :root {
        --mobile: 320px;
        --tablet: 768px;
        --desktop: 1024px;
    }

    /* Sử dụng nhất quán */
    @media (min-width: 768px) {...}
8. Comments và Documentation:
    /* ==========================================================================
    Header Component
    ========================================================================== */

    /**
    * 1. Đảm bảo header luôn nổi trên cùng
    * 2. Thêm shadow để tạo depth
    */

    .header {
        z-index: 100; /* 1 */
        box-shadow: 0 2px 4px rgba(0,0,0,.1); /* 2 */
    }
9. Tối ưu Selector:
    /* Tránh selector quá sâu */
    /* Không tốt */
    .header .nav .menu .item {...}

    /* Tốt */
    .header__menu-item {...}
10. Sử dụng CSS Modern Features:
    /* Sử dụng CSS Grid và Flexbox */
    .layout {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
    }

    /* Sử dụng CSS Custom Properties */
    .element {
        color: var(--primary-color);
        transition: var(--transition-base);
    }
11. Maintainable & Scalable:
    /* Tách components */
    @import 'components/header';
    @import 'components/navigation';
    @import 'components/buttons';

    /* Sử dụng utility classes */
    .d-flex { display: flex; }
    .align-center { align-items: center; }
12. File Organization:
    styles/
    ├── base/
    │   ├── _reset.css
    │   ├── _typography.css
    │   └── _variables.css
    ├── components/
    │   ├── _header.css
    │   └── _navigation.css
    ├── layouts/
    │   └── _grid.css
    ├── utilities/
    │   └── _helpers.css
    └── main.css

Áp dụng các best practices này sẽ giúp:
    - Code dễ bảo trì và mở rộng
    - Tối ưu performance
    - Cải thiện accessibility
    - Dễ dàng làm việc theo team
    - Giảm thiểu conflicts khi merge code
    - Tăng tính tái sử dụng của code